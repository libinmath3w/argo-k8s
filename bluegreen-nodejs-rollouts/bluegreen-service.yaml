apiVersion: v1
kind: Service
metadata:
  name: bluegreen-nodejs-demo
  labels:
    app: bluegreen-nodejs-demo
spec:
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: bluegreen-nodejs-demo